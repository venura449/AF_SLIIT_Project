# NIC Document Uploads

This directory stores uploaded NIC (National Identity Card) documents from users.

Files are stored with the naming convention: `{userId}_{timestamp}{extension}`

**Note:** This directory should not be committed with actual documents. Only the .gitkeep and README are version controlled.
